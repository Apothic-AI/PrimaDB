# Local Notes

This example writes notes, bytes, and blobs through the native Node addon and stores them in a
package-local data directory.

## Run

```bash
cd /home/bitnom/Code/gunport/primadb/packages/primadb-node
pnpm install
pnpm run build
node ./examples/local-notes/index.mjs
```
